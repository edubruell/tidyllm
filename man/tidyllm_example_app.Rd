% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_app.R
\name{tidyllm_example_app}
\alias{tidyllm_example_app}
\title{Run a tidyllm example Shiny app}
\usage{
tidyllm_example_app(.app = NULL, ...)
}
\arguments{
\item{.app}{Name of the app to run. With no argument the available apps are
listed.}

\item{...}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
Invisibly, the path to the app that was run.
}
\description{
The apps ship with the installed package, so they always match the version of
tidyllm you have. Each one defaults to a local \code{ollama()} model and therefore
runs with no API key and no spend; a dropdown switches to the cloud providers.
}
\details{
Available apps:

\code{"model_explainer"}: fits a linear model to a small public dataset and streams
two explanations of the coefficients side by side, one plain-English and one
adversarial. Every number is computed in R; the model only narrates the
coefficient table it is shown.
}
\examples{
\dontrun{
tidyllm_example_app()
tidyllm_example_app("model_explainer")
}

}
