% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async_chat.R
\name{get_stream}
\alias{get_stream}
\title{A running chat's deltas, as a stream}
\usage{
get_stream(.job)
}
\arguments{
\item{.job}{A streaming \code{tidyllm_chat_job} from \code{\link[=send_chat]{send_chat()}}.}
}
\value{
A \code{coro} async generator instance yielding character deltas.
}
\description{
Returns a \code{coro} async generator over the text deltas: the pull form of
\code{.on_chunk}. It replays whatever has already arrived and then continues live,
so it is safe to ask for at any point in a job's life and always tells the
same story as \code{\link[=get_partial]{get_partial()}}.
}
\details{
This is what \code{shinychat::chat_append()} consumes directly, because
shinychat's whole extensibility contract is
\code{inherits(x, "coro_generator_instance")}. The generator is asynchronous
rather than synchronous on purpose: shinychat's consumer only yields the
event loop at an \code{await()}, so a synchronous generator would drain in one
tick and the reply would appear all at once instead of token by token.

In a plain script this is rarely what you want. An async generator hands a
\code{coro::loop()} promises rather than text, so consuming it takes
\code{coro::async()} and \code{coro::await_each()}; \code{.on_chunk} or a \code{get_partial()}
loop says the same thing with less ceremony.
}
\examples{
\dontrun{
job <- llm_message("Tell me a story") |> send_chat(claude(), .stream = TRUE)
shinychat::chat_append("chat", get_stream(job))
}

}
