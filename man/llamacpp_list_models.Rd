% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_llamacpp.R
\name{llamacpp_list_models}
\alias{llamacpp_list_models}
\title{List Models Loaded in the llama.cpp Server}
\usage{
llamacpp_list_models(
  .server = Sys.getenv("LLAMACPP_SERVER", "http://localhost:8080"),
  .api_key = Sys.getenv("LLAMACPP_API_KEY", ""),
  .timeout = 30,
  .max_tries = 3
)
}
\arguments{
\item{.server}{Base URL of the llama.cpp server. Defaults to \code{LLAMACPP_SERVER}
env var, falling back to \code{"http://localhost:8080"}.}

\item{.api_key}{API key for the server (default: \code{LLAMACPP_API_KEY} env var).}

\item{.timeout}{Request timeout in seconds (default: 30).}

\item{.max_tries}{Maximum retries (default: 3).}
}
\value{
A tibble with columns \code{id}, \code{object}, and \code{created}.
}
\description{
Calls the \verb{/v1/models} endpoint of a running llama.cpp server and returns
the currently loaded model(s) as a tibble. In normal operation this is one
row; two rows appear when speculative decoding is active (main + draft model).
}
